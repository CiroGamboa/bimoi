# Code quality, tests, and pre-commit (install from repo root: pip install -e ".[dev]")
# Or: pip install -e . && pip install -r requirements-dev.txt
pre-commit>=4.0.0
pytest>=8.0
testcontainers[neo4j]>=4.0
neo4j>=5.0
