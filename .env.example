# Backend (FastAPI + Telegram webhook) and Neo4j. Copy to .env and set values.
# Get TELEGRAM_BOT_TOKEN from @BotFather.
# For production: run backend (Docker or uvicorn api.main:app), set webhook to https://<domain>/webhook/telegram.
# For local dev without a public URL: set USE_POLLING=1 and run python -m bot.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
TELEGRAM_BOT_TOKEN=
# USE_POLLING=1
