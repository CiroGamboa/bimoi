# Backend (FastAPI + Telegram webhook) and Neo4j. Copy to .env and set values.
# Get TELEGRAM_BOT_TOKEN from @BotFather.
# Production: run backend, set webhook to https://<domain>/webhook/telegram.
# Local dev: run backend, ngrok http 8010, then python scripts/set_webhook_ngrok.py
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
TELEGRAM_BOT_TOKEN=
